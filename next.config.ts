// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "images.unsplash.com",
      },
      {
        protocol: "https",
        hostname: "**", // ← si tu backend sirve avatares/portfolio desde dominios variables
      },
    ],
  },
};

export default nextConfig;